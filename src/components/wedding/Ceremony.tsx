import { MapPin, Clock, Shirt, Car } from "lucide-react";import { wedding, timeline } from "@/lib/wedding-data";import coupleDinner from "@/assets/couple-dinner.jpg.asset.json";import { Reveal } from "./Reveal";import { assetUrl } from "@/lib/asset-url";const details = [  { icon: MapPin, label: "Local", value: wedding.venueAddress },  { icon: Clock, label: "Horário", value: `${wedding.dateLabel}, às ${wedding.timeLabel}` },  { icon: Shirt, label: "Traje", value: wedding.dressCode },  { icon: Car, label: "Estacionamento", value: "Informações a definir mais próximo do dia" },];export function Ceremony() {  return (    <>      <section className="bg-cream py-20 md:py-24">        <div className="mx-auto grid max-w-5xl items-center gap-10 px-5 md:grid-cols-[0.8fr_1fr]">          <Reveal>            <div className="surface mx-auto w-full max-w-sm overflow-hidden p-2">              <img                src={assetUrl(coupleDinner.url)}                alt="Jessica e Rillan em um jantar especial a dois"                loading="lazy"                className="w-full rounded-[0.9rem] object-contain"              />            </div>          </Reveal>          <Reveal delay={120}>            <p className="text-center font-serif text-3xl italic leading-snug text-sage-deep md:text-left md:text-4xl">              “Assim, eles já não são dois, mas uma só carne.” — Mateus 19:6            </p>          </Reveal>        </div>      </section>
